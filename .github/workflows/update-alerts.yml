name: update-alerts

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  refresh-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh alert data
        env:
          OREF_HIST_URL: ${{ secrets.OREF_HIST_URL }}
        run: python scripts/fetch_alerts.py --output-status

      - name: Copy data into Pages artifact
        run: |
          mkdir -p docs/data
          cp data/alerts.json docs/data/alerts.json
          cp data/alerts-recent.json docs/data/alerts-recent.json
          cp data/metadata.json docs/data/metadata.json

      - name: Commit generated data
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add data docs/data
          if git diff --cached --quiet; then
            echo "No data changes to commit"
          else
            git commit -m "Update alerts data"
          fi

      - name: Push changes
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy Pages
        uses: actions/deploy-pages@v4
